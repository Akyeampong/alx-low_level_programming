#include <stdio.h>

/**
 * main - Alphabets from a to z
 * 
 * Return: Always 0 (Success)
 */

int main(void)
{
	char lo;

	for (lo = 'a'; lo <= 'z'; lo++)
		putchar(low);
	putchar('\n');

	return (0);
}
